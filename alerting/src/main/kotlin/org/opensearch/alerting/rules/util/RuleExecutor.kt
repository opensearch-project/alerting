/*
 * Copyright OpenSearch Contributors
 * SPDX-License-Identifier: Apache-2.0
 */

package org.opensearch.alerting.rules.util

object RuleExecutor {
    fun getSuggestions(obj: Any): List<String> {
//        val ruleFactory = RuleFactory()
//        val suggestions = mutableListOf<String>()
//        for (registeredRule in RuleFactory.RegisteredRule.values()) {
//            val rule = ruleFactory.getRule(registeredRule)
//            val passedCheck = rule.evaluate(monitor)
//            if (!passedCheck) {
//                suggestions.add(rule.suggestion())
//            }
//        }
        return listOf("placeholder suggestion")
    }
}
